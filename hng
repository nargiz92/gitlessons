// Setup
var a;

// Only change code below this line
a=7;
<div>I am Nargiza</div>
<div>
    <ul>
        <li>git clone </li>
        <li>git status</li>
        <li>git commit</li>
        <li>git commitgit</li>
        <li> We started to learn js from https://learn.javascript.ru/types</li>
        <script>
          "use script"; 
          alert( 1/0 );
          alert( "не число"/2);
          alert( "не число прилипчиво"/6+9);
         
        </script>
        <script> //bigInt числа больше чем 9007199254740991 (2,53 -1) или меньше-(-2,53 -1)
         // n в конце значения означает BigInt
         const BigInt= 90055665656565654545454545n;
         let str = "привет";
         let str = 'привет, строка с одинарной кавычкой';
         let phrase =`Привет`;
         let name ="Ivan";
         //вставим переменную
         alert(`Hello, ${name}!`);
        </script>
        <script>
            let name ="Ivan";
         //вставим переменную
         alert(`Hello, ${name}!`);
           
         //вставим выражение
         alert( `результат: ${1+2}`); 
        </script>
        </script>
    </ul>
</div>
  //вставим переменную
         alert(`Hello, ${name}!`);
        </script>
        <script>
            let name ="Ivan";
         //вставим переменную
         alert(`Hello, ${name}!`);
           
         //вставим выражение
         alert( `результат: ${1+2}`); 
        </script>
        </script>
        <script>
            //булевый (логический) тип
            let nameFieldChecked = true;
            let ageFieldChecked = false;
            let isGreater = 4>1;

            alert(
                isGreater 
            );
            let age;
            alert(age);
            // null используется для присвоения переменой пустого или неизвестного значения
            //undefined используется для проверки была ли переменная назначенна
            //объект object тип данных или особенный тип, все остальные типы данных примитивные
            //symbol - используются для создания уникальных индификаторов
            //typeOf- возвращает тип аргумента, тва способа вызова typeOf x; typeOf(x); 
            /* Вызов typeof x возвращает строку с именем типа */
        </script>
        <script>
            typeof undefined; 
            typeof 0;
            typeof 10n;
            typeof null;
            typeof 'cool';
            typeof true;
            typeof Math;
            typeof alert;
            typeof Symbol;
 
        </script>
        // задача
        <script>
            "use strict";
            let name = "Ilya";
            alert(`hello ${1}`);
            alert( `hello ${"name"}`);
            alert(`hello ${name}`);
        </script>
        <script>
     "use strict";

     //alert это функция или модальное окно, означающий что пользователь не может взаимодействовать с интерфейсом браузера пока не отргеагирует на алерт и нажмет ок
      // promt принимает два аргумента title - текст для отображения в окне and default;
let age = prompt('How old are you', 100);
alert(`тебе ${age} лет!`);
      //confirm показывает сообщения и ждет пока пользователь нажмет ок или отмена, возвращает true, если нажато ок, или false если нажата кнопка отмена или Еsc
      let isBoss = confirm("Ты здесь главный");
      alert( isBoss );
      let userName= prompt("как тебя зовут");
      alert(`я ${userName}.`);
      //преобразование типов три способа: строковое, числовое, логическое преобразовании
  
      let value =true;
      alert(typeof value);
      value =String(value);
      alert(typeof value);
      let str ="123";
      alert(typeof str);
      let num = Number(str);
      alert(typeof num);
      let age = Number("Любая строка вместо числа");
      alert(age);
 </script>
